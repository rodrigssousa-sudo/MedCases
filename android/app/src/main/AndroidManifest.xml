<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.medcasespro.med">

    <!-- INTERNET: obrigatória para Firebase, HTTP REST, Firestore, Auth -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- RECORD_AUDIO: necessária para speech_to_text (ditado por voz nativo) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- POST_NOTIFICATIONS: necessária no Android 13+ para notificações do sistema -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!--
        EXACT ALARMS — BLOQUEIO TOTAL (Build 114):
        tools:node="remove" expurga estas permissões do manifesto final mesclado,
        incluindo quaisquer declarações injetadas silenciosamente por SDKs de terceiros
        (flutter_local_notifications, WorkManager, Firebase Messaging, etc.).
        Sem estas permissões, a Google Play Store não exige revisão manual de alarmes
        exatos — reduzindo fricção de publicação e rejeições de política.
        O app continua funcionando com notificações inexatas (suficientes para uso clínico).
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"      tools:node="remove"/>

    <!-- RECEIVE_BOOT_COMPLETED: reagenda notificações após reboot do aparelho -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!--
        BUILD 314: android:networkSecurityConfig referencia o XML que define
        a política de rede por domínio. Cleartext HTTP desabilitado globalmente
        via base-config — apenas localhost/10.0.2.2 têm exceção para dev.
        android:usesCleartextTraffic REMOVIDO (era BUILD 313 — causava conflito
        com o canal gRPC TLS do Firestore SDK no Android, bloqueando conexões).
    -->
    <application
        android:label="MedCases Pro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- flutter_local_notifications: receiver para exact alarms -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"/>
            <!-- PROFILE_ACCOUNT_HUB_PREMIUM_V1_B_R2 — image_cropper -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

            <service
            android:name=".MedCasesRecordingForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
            <service
            android:name=".MedCasesStudyBackgroundTranscriptionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- share_plus: necessário para ACTION_SEND no Android 11+ -->
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: necessário para abrir links externos -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
